<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Xml | I'm Ricky -- 个人主页]]></title>
  <link href="http://rickytan.cn/blog/categories/xml/atom.xml" rel="self"/>
  <link href="http://rickytan.cn/"/>
  <updated>2016-10-28T21:21:15+08:00</updated>
  <id>http://rickytan.cn/</id>
  <author>
    <name><![CDATA[Ricky]]></name>
    <email><![CDATA[ricky.tan.xin@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[PHP 中读 XML 的一个坑]]></title>
    <link href="http://rickytan.cn/blog/2014/06/05/the-php-xml-issue/"/>
    <updated>2014-06-05T01:47:00+08:00</updated>
    <id>http://rickytan.cn/blog/2014/06/05/the-php-xml-issue</id>
    <content type="html"><![CDATA[<h2>问题</h2>

<p>在做一个微信的 <code>PHP</code> 后台时需要读微信服务器 <code>POST</code> 过来的 <code>XML</code>，然后保存到 <code>Memcache</code> 中。</p>

<p>{% codeblock index.php lang:php %}
&lt;?php
public index() {</p>

<pre><code>$postData = $GLOBALS["HTTP_RAW_POST_DATA"];
$postObj = simplexml_load_string($postStr, 'SimpleXMLElement', LIBXML_NOCDATA);
$this-&gt;cache-&gt;memcached-&gt;save("openid_".$something, $postObj-&gt;FromUserName);

...
</code></pre>

<p>}
?>
{% endcodeblock %}</p>

<p>怎么也出不来结果，然后发现运行到 <code>save</code> 时有异常<!--more-->，是 <code>Memcache</code> 抛出的，说无法串行化。通过 <code>var_dump</code> 后发现 <code>$postObj-&gt;FromUserName</code> 是个 <code>SimepleXMLElement</code> 类型，而用 <code>var_dump</code> <code>$postObj</code> 时发现是 <code>string</code>！！</p>

<h2>解决方法</h2>

<p>解决方法有多种:</p>

<ol>
<li>强制转换为 <code>string</code></li>
</ol>


<p>{% codeblock lang:php %}
&lt;?php</p>

<pre><code>    $this-&gt;cache-&gt;memcached-&gt;save("openid_".$something, (string)$postObj-&gt;FromUserName);
</code></pre>

<p>?>
{% endcodeblock %}</p>

<ol>
<li>强制转换为 <code>array</code></li>
</ol>


<p>{% codeblock lang:php %}
&lt;?php</p>

<pre><code>    $postData = $GLOBALS["HTTP_RAW_POST_DATA"];;
    $postObj = (array)simplexml_load_string($postStr, 'SimpleXMLElement', LIBXML_NOCDATA);
    $this-&gt;cache-&gt;memcached-&gt;save("openid_".$something, $postObj["FromUserName"]);
</code></pre>

<p>?>
{% endcodeblock %}</p>

<ol>
<li>转为 <code>stdClass</code></li>
</ol>


<p>{% codeblock lang:php %}
&lt;?php</p>

<pre><code>    $postObj = simplexml_load_string($postStr, 'SimpleXMLElement', LIBXML_NOCDATA);
    $postObj = json_decode(json_encode($postObj));
</code></pre>

<p>?>
{% endcodeblock %}</p>

<p>这个大坑耗去了快两小时！</p>
]]></content>
  </entry>
  
</feed>
